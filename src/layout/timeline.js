const LOGO_WIDTH = 65;
const LOGO_HEIGHT = 80;

const PADDING_TOP = 10;

export const getUsersTop = () => LOGO_HEIGHT;
export const getFirstUserTop = () => LOGO_HEIGHT + PADDING_TOP;
export const getHeaderLeft = () => LOGO_WIDTH;
